---
import { Image } from 'astro:assets';
import BaseLayout from '../layouts/BaseLayout.astro';

import profilePicture from '../images/portrait_transbg_nooutline.png';
import LinkList from '../components/HeroLinks.astro';

const title = 'Austin Hagel';
---

<style>
  h1 {
    margin: 0;
    padding: 0;
  }

  .hero-wrapper {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 1rem;

    padding-inline: 1rem;
    padding-block: 8rem;
  }

  .hero-info-wrapper {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
  }

  .profile-image {
    border-radius: 50%;
    outline: 2px solid var(--clr-text);

    background-color: var(--clr-surface-0);

    --image-size: 12rem;

    width: var(--image-size);
    height: var(--image-size);

    box-shadow: 0px 8px 24px rgba(0, 0, 0, 0.25);
  }

  h1.hero-title {
    font-size: 2rem;
    font-weight: 400;
    letter-spacing: 0.02ch;

    padding: 0;
    margin: 0;
  }

  .hero-paragraph {
    max-width: 74ch;
    font-size: 1rem;

    padding: 0;
    margin: 0;
  }

  .hero-link-list {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 100%;

    margin-top: 2rem;
  }

  .hero-content-wrapper {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 1rem;
  }

  @media screen and (min-width: 768px) {
    .hero-wrapper {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 8rem;
    }

    .profile-image {
      --image-size: 18rem;
    }

    h1.hero-title {
      font-size: 2.6rem;
    }

    .hero-content-wrapper {
      grid-row: 1;
      justify-self: end;
      margin-top: 2.4rem;
    }

    .hero-paragraph-wrapper {
      grid-row: 2;
      grid-column-start: 1;
      grid-column-end: -1;
    }

    .hero-info-wrapper {
      grid-row: 1;
      width: 20rem;
      justify-self: start;
    }
  }
</style>

<BaseLayout title={title}>
  <section class='hero-wrapper max-width'>
    <div class='hero-content-wrapper'>
      <Image
        src={profilePicture}
        alt='A photo of Austin'
        loading='lazy'
        decoding='async'
        width='512'
        class='profile-image'
      />
      <h1 class='hero-title'>Austin Hagel</h1>
    </div>

    <div class='hero-paragraph-wrapper'>
      <p>
        Lorem ipsum dolor sit amet consectetur adipisicing elit. Quisquam,
        accusantium commodi quaerat error ex sint provident enim exercitationem
        deserunt incidunt sed, consectetur reprehenderit id, laboriosam fugiat
        nemo voluptatum optio nisi.
      </p>
    </div>

    <div class='hero-info-wrapper'>
      <div class='hero-link-list'>
        <LinkList />
      </div>
    </div>
  </section>
</BaseLayout>
